# =============================================================================
# Cloudflare Tunnel — Environment Variables
# =============================================================================
# cp .env.cloudflared.example .env.cloudflared
#
# docker-compose.yml / docker-compose.prod.yml の cloudflared サービスが読み込む。
# =============================================================================

TUNNEL_TOKEN=your-cloudflare-tunnel-token-here
